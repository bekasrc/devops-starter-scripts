USERNAME=$1
PORT=$2
if [ $# -lt 2 ]; then
    # -lt означает "меньше чем" (less than)
echo "❌ ОШИБКА: Не переданы все аргументы. Нужно: Имя, Порт."
exit 1
elif [ "$2" -le 1024 ]; then
    # ИЛИ elif [ "$2" -le 1024 ]; then
    # -le означает "меньше или равно" (less than or equal)
echo "❌ ОШИБКА: Порт $2 зарезервирован. Используйте порт > 1024"
exit 1
else
echo "✅ Конфигурация успешна: Пользователь $1, Порт $2"
exit 0
fi

